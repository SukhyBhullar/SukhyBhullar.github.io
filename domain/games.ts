export type Game = {
  callSign: string;
  firstName: string;
  lastName: string;
};

export type id = {
  id: string;
};

export type userId = {
  userId: string;
};

export type Id = {
  id: string;
};

export type currentPlace = {
  currentPlace: string;
};
